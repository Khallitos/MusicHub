const fromFile = require('./fromFile')
const fromURL = require('./fromURL')
const mimeType = require('./mimeType')
const storageSize = require('./storageSize')

const getBase64 = {
  fromFile,
  fromURL,
  mimeType,
  storageSize
}

module.exports = getBase64