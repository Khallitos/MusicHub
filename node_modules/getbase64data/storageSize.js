const storageSize = async (base64String) => {
    const strImage = base64String.replace(/^data:image\/[a-z]+;base64,/,"");

    const fileSize = strImage.length
    return fileSize;
}

module.exports = storageSize